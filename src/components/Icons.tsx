import {
  ArrowRight,
  Check,
  HeartCrack,
  Loader2,
  LogOut,
  X,
  Zap,
} from "lucide-react";

export const Icons = {
  check: Check,
  loading: Loader2,
  error: X,
  zap: Zap,
  arrowRight: ArrowRight,
  logout: LogOut,
  heartBreak: HeartCrack,
};

export type Icon = typeof Icons;
